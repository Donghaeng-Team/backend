spring:
  application:
    name: user-service
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}
  config:
    import: ${SPRING_CONFIG_IMPORT:configserver:http://localhost:8888}
  datasource:
    url: jdbc:postgresql://localhost:5432/user_db
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: validate
  data:
    redis:
      host: redis
      port: 6379
  mail:
    host: smtp.gmail.com
    port: 587
    username: bytogethermaster@gmail.com
    password: zcjxbucnbljywuai
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
  security:
    jwt: a23djklfjklsjd234ksldjfl97sdhjkhjk7jkl8bnyl5sh0asdfemdfhbG9uZ2Vub3VnaGZvcmhzMjU2
    access: 1
    refresh: 7

    #Kakao Oauth2 ??
    oauth2:
      client:
        registration:
          kakao:
            client-id: { KAKAO_CLIENT_ID }
            client-secret: { KAKAO_CLIENT_SECRET }
            client-authentication-method: client_secret_post
            authorization-grant-type: authorization_code
            redirect-uri: "{baseUrl}/login/oauth2/code/{registrationId}"
            scope:
              - profile_nickname
              - account_email
        provider:
          kakao:
            authorization-uri: https://kauth.kakao.com/oauth/authorize
            token-uri: https://kauth.kakao.com/oauth/token
            user-info-uri: https://kapi.kakao.com/v2/user/me
            user-name-attribute: : { USER_ID }
